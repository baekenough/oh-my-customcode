[install]
# Faster installs
auto-install-peers = true

[test]
# Coverage configuration
coverage = true
coverageDir = "coverage"
coverageReporter = ["text", "lcov"]
# CI tooling, not shipped code — exclude from the coverage gate.
coveragePathIgnorePatterns = [".github/**"]

[run]
# Bun runtime options
smol = true
