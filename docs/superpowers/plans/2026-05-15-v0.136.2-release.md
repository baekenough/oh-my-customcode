# 릴리즈 계획 — 2026-05-15 생성

> 출처: 2026-05-15 기준 `verify-done` 라벨 오픈 이슈
> Milestone: v0.136.2 (#41)
> 제외된 이슈 (이미 오픈 PR에 포함): 없음

## v0.136.2 릴리즈 계획

**예상 범위**: M+M+S+S+S (≈ L 누적) | **이슈**: 5 | **병렬 가능**: 5 (의존성 없음)
**테마**: Release pipeline 견고화 — process-gap 5건 묶음
**Version bump**: 0.136.0 → 0.136.2 (patch, v0.136.1 태그는 npm publish 실패 상태로 skip)

| # | 우선순위 | 규모 | 제목 | 의존성 |
|---|----------|------|------|--------|
| #1154 | P2 | M | release 단계 package.json/manifest.json version bump 누락 | 없음 (자기참조) |
| #1155 | P3 | M | guides 추가 시 manifest.json + README guides count 자동 sync 부재 | 없음 |
| #1144 | P3 | S | templates/.claude/skills/pipeline/workflows/auto-dev.yaml Sensitive Path 섹션 drift | 없음 |
| #1145 | P3 | S | wiki/index.yaml total_pages/skills 카운트 드리프트 | 없음 |
| #1152 | P3 | S | pre-commit jq 의존성 — 설치 가이드 또는 graceful skip | 없음 |

### 구현 순서

1. **#1154 — auto-dev.yaml release 단계에 package.json + templates/manifest.json version bump 명시** (권장 에이전트: mgr-updater)
   - 파일: `.claude/skills/pipeline/workflows/auto-dev.yaml` line 149-151
   - 변경: release prompt에 `jq '.version = "{NEW}"' package.json` + `templates/manifest.json` bump + pre-tag commit 명시
   - 검증: `scripts/verify-version-sync.sh` 호출 추가
   - 동시에 `templates/.claude/skills/pipeline/workflows/auto-dev.yaml` 동기화

2. **#1155 — verify-template-sync.sh에 guides count 일치 검사 추가** (권장 에이전트: mgr-updater)
   - 파일: `.github/scripts/verify-template-sync.sh`
   - 변경: `manifest.json` files.guides 와 실제 `guides/*/` 디렉토리 카운트 비교 추가
   - 부가: `templates/README.md` 의 `### Guides (N)` 헤딩 카운트도 검증

3. **#1144 — templates auto-dev.yaml Sensitive Path 섹션을 source와 동기화** (권장 에이전트: mgr-updater)
   - 파일: `templates/.claude/skills/pipeline/workflows/auto-dev.yaml` line 91
   - 변경: deprecated `/tmp/*.sh` 패턴 → CC v2.1.121+ `bypassPermissions` 패턴
   - 정합성: source `.claude/skills/pipeline/workflows/auto-dev.yaml`와 1:1 일치 확인
   - 비고: #1154 fix 시 source가 추가 변경되므로 #1154 이후 진행 권장 (그러나 둘 다 동시 가능 — sync 작업이라 conflict 없음)

4. **#1145 — wiki/index.yaml 카운트 재생성** (권장 에이전트: wiki-curator)
   - 파일: `wiki/index.yaml`
   - 변경: `total_pages`, `counts.{skills,agents,rules,guides}` 실제 값으로 업데이트
   - 검증: `find wiki -name '*.md' | wc -l` 결과와 일치
   - 부가: verify-wiki-sync.sh에 카운트 일치 검사 추가

5. **#1152 — CONTRIBUTING.md에 jq prerequisite 명시** (권장 에이전트: arch-documenter)
   - 파일: `CONTRIBUTING.md` (없으면 신규)
   - 변경: prerequisites 섹션에 jq 추가 + 설치 명령 (apt/brew/static binary)
   - 비고: hook script 본체는 이미 graceful skip (line 13) — 문서만 필요

### 병렬 실행 가능성

5개 이슈 모두 의존성 없음. 단, 파일 충돌 회피를 위해:
- #1154 + #1144: 같은 auto-dev.yaml family 수정 — 같은 mgr-updater 직렬 처리 권장
- #1155 + #1145: `verify-*-sync.sh` family — 같은 mgr-updater 직렬 처리 권장
- #1152: 완전 독립 — 병렬 가능

권장 dispatch 패턴:
- Wave 1 (병렬): #1154 (mgr-updater), #1145 (wiki-curator), #1152 (arch-documenter)
- Wave 2 (병렬): #1144 (mgr-updater, after Wave 1 #1154), #1155 (mgr-updater)

### 참고 사항

- **자기참조 fix**: #1154 fix는 이 릴리즈 자체에 적용 — 이번 v0.136.2 릴리즈가 npm publish를 성공시키려면 #1154 fix가 release 단계 실행 전에 commit되어야 함
- **버전 충돌 회피**: v0.136.1 태그는 npm 미발행 상태이므로 0.136.1 재사용 위험. 0.136.2로 점프
- **post-mortem 학습 적용**: v0.136.1 회고에서 86 test failures는 pre-existing baseline — verify-build에서 정확히 86 failures 시 통과로 처리 (regression 아님)

## 요약

| 릴리즈 | 이슈 수 | 규모 | P1 | P2 | P3 |
|--------|---------|------|----|----|-----|
| v0.136.2 | 5 | M+M+S+S+S | 0 | 1 | 4 |

전체 P1: 0, P2: 1, P3: 4
