# 릴리즈 계획 — 2026-04-25 생성

> 출처: 2026-04-25 기준 `verify-done` 라벨 오픈 이슈
> 제외된 이슈 (이미 오픈 PR에 포함): 없음
> 현재 버전: 0.109.0 → 0.110.0 (minor bump — 신규 user-facing 레이어 + 신규 패턴 추가)

## v0.110.0 릴리즈 계획

**예상 범위**: S + M = M | **이슈**: 2 | **병렬 가능**: 2 (의존성 없음)

| # | 우선순위 | 규모 | 제목 | 의존성 |
|---|----------|------|------|--------|
| #1003 | P2 | S | [scout:integrate] Claude Code Output Styles — 세션 레벨 프롬프트 레이어 도입 검토 | 없음 |
| #1007 | P2 | M | [scout:internalize] cc-roundtable — Devil's Advocate + anti-groupthink 다중 에이전트 토론 패턴 | 없음 |

### 구현 순서

1. **#1003 — Claude Code Output Styles 도입 (S)** (권장 에이전트: mgr-creator + update-config)
   - `.claude/output-styles/korean-engineer.md` 신규 작성 (`keep-coding-instructions: true`)
   - `templates/.claude/output-styles/korean-engineer.md` 동기화 (3중 동기화 패턴)
   - `settings.local.json`에 `outputStyle: "korean-engineer"` 추가 (update-config 스킬)
   - R003 `SHOULD-interaction.md`에 "세션 레벨 강제화는 Output Styles로 위임" 주석 추가
   - 옵션: `review-mode.md`, `onboarding.md` 추가 작성 (P3 — 본 릴리즈에서는 제외)
   - wiki: `wiki/guides/output-styles.md` 생성 (R022)

2. **#1007 — cc-roundtable anti-groupthink 패턴 내재화 (M)** (권장 에이전트: mgr-creator + mgr-updater + arch-documenter)
   - 신규 스킬 `roundtable-debate` 생성 (mgr-creator)
     - scope: core, context: fork (현재 9/12 → 10/12, 캡 도달 안 함)
     - 핵심 메커니즘 4종: Independent-first parallel analysis, Devil's Advocate 강제 주입, 소수의견 보호 프로토콜, 2라운드 하드캡
     - `agora`와의 차별점: **발산 보존(divergence preservation) vs 수렴(convergence)**
   - 기존 스킬 `agora` 보강 (mgr-updater)
     - "anti-groupthink mode" 옵션 추가 (Devil's Advocate 슬롯 + 소수의견 보호 룰)
     - cc-roundtable 출처 attribution (R016)
   - 신규 가이드 `guides/multi-agent-debate-patterns/` 작성 (arch-documenter)
     - Anchoring / Groupthink / Degeneration of Thought 정의
     - `agora` vs `roundtable-debate` 선택 매트릭스
     - 연구 근거 (2-round cap rationale)
   - CLAUDE.md "권장 플러그인" 섹션에 cc-roundtable 추가 (mgr-updater)
   - wiki 동기화: `wiki/skills/roundtable-debate.md`, `wiki/skills/agora.md` 갱신, `wiki/guides/multi-agent-debate-patterns.md` 생성 (R022)

### 참고 사항

**fork cap 검증**: `verify-fork-list.sh` 실측 9/12 (3슬롯 여유). #1007 이슈 본문의 "12/12 도달" 표기는 부정확 — 신규 fork 추가 가능, 기존 비-fork 전환 불필요. 이는 R016 후속 학습 항목.

**카운트 동기화 필수 (7곳)**: CLAUDE.md, templates/CLAUDE.md, README.md, README_ko.md, ARCHITECTURE.md(2), templates/manifest.json
- skills 113 → 114 (roundtable-debate 추가)
- guides 44 → 45 (multi-agent-debate-patterns 추가)
- output-styles는 카운트 항목이 아니므로 별도 동기화 불필요 (필요 시 ARCHITECTURE.md에 신규 섹션)

**`/research` 선행 권고 (#1007)**: professor-triage가 cc-roundtable의 학술 근거 + 메커니즘 차별성 확인을 위해 `/research https://github.com/gaebalai/cc-roundtable` 권장. deep-plan 단계에서 통합 가능.

**sensitive-path 우회**: `.claude/` 편집 시 `/tmp/*.sh` 스크립트 + Bash 실행 패턴 사용 (auto-dev v0.109.0 공식화).

**CI 검증 (implement 후 필수)**:
- `bash .github/scripts/verify-template-sync.sh`
- `bash .github/scripts/verify-wiki-sync.sh`
- `bash .github/scripts/verify-fork-list.sh`

## 요약

| 릴리즈 | 이슈 수 | 규모 | P1 | P2 | P3 |
|--------|---------|------|----|----|-----|
| v0.110.0 | 2 | M (S+M) | 0 | 2 | 0 |

**카운트 변경 예측**:
- skills: 113 → 114 (+1: roundtable-debate)
- agents: 49 (변동 없음)
- rules: 22 (R003 주석 추가만)
- guides: 44 → 45 (+1: multi-agent-debate-patterns)
- wiki pages: 257 → 260+ (스킬/가이드/룰 페이지 동기화)
- hooks: 32 (변동 없음)
