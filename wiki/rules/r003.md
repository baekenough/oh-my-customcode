---
title: "R003: Interaction Rules"
type: rule
updated: 2026-09-18
sources:
  - .claude/rules/SHOULD-interaction.md
related:
  - [[r000]]
  - [[r004]]
  - [[r013]]
---

# R003: Interaction Rules

Defines response style, status format, and request handling behavior for all agent responses.

## Overview

Ensures consistent, actionable communication. Responses should be brief and specific. Output verbosity adapts based on task complexity and active modes (ecomode, effort level). Status reporting uses a standard format so users can track progress uniformly.

## Priority

**SHOULD** — Strong recommendation. Violations reduce communication quality but do not block operations.

## Key Requirements

**Status format:**
```
[Start] {task name}
[Progress] {current step} ({n}/{total})
[Done] {task name} — Result: {summary}
[Failed] {task name} — Cause: {reason} — Alternative: {solutions}
```

**Request handling:**

| Type | Action |
|------|--------|
| Clear | Execute immediately |
| Ambiguous | `[Confirm] Understood "{request}" as {interpretation}. Proceed?` |
| Risky | `[Warning] This action has {risk}. Continue? Yes: {action} / No: Cancel` |
| Interrupt (ambiguous first message) | Do NOT assume the prior request is cancelled — confirm intent first. The first message after an interrupt may be continuation, correction, or a new request. See [[r020]] "Interrupt ≠ Prior-Request Cancellation" (#1341). **Exception:** if the in-progress action is Risky (destructive/irreversible), apply the Risky row first (immediate halt). Normative definition and Safety Carve-Out owned by [[r020]]. |

**Request handling precedence: Risky > Interrupt > Ambiguous > Clear.** 파괴적 작업 진행 중 인터럽트가 발생하면 의도 모호 여부와 무관하게 Risky 행(즉시 중단, [[r001]]/[[r020]] Safety Carve-Out)이 우선 적용된다. 명확한 새 지시는 Clear 경로로 즉시 처리한다.

**Output style selection priority:**
1. User explicit request → override
2. Ecomode active ([[r013]]) → force `concise`
3. Agent effort level → map to style (`low`=concise, `medium`=balanced, `high`=explanatory)
4. Default → `balanced`

## Session-Level Style Enforcement

Per-response style selection (concise/balanced/explanatory) is R003's responsibility. Session-wide default tone and format are handled by the Output Styles layer — a separate CC-native mechanism.

| Layer | Responsibility | Trigger |
|-------|---------------|---------|
| R003 (this rule) | Per-response style criteria | Prompt-based, advisory |
| R013 ([[r013]]) | Ecomode concise override | Dynamic, context-triggered |
| Output Styles | Session-wide tone/format | Static, session-level |

Default active style: `korean-engineer` (`.claude/output-styles/korean-engineer.md`). See [output-styles guide](../guides/output-styles.md).

**`/output-style [name]` remediation (v2.1.269+):** `/output-style [name]` can now list and switch output styles mid-session, and works in Remote Control and cloud/headless sessions. This is a direct remedy for the v2.1.238-era drift above — mid-session `/output-style korean-engineer` re-selects/re-confirms this repo's custom style, and gives `-p`/cloud runs a way to guarantee the style is actually active rather than relying on the default.

## Enforcement

Prompt-based (CLAUDE.md + rules/) per [[r021]]. Advisory only.

## Relationships

- **Related rules**: [[r000]], [[r004]], [[r013]], [[r020]]
- **See also**: [output-styles](../guides/output-styles.md)
- **Affects**: All agent responses, status reporting across all workflows

## Sources

- `.claude/rules/SHOULD-interaction.md` — rule definition (Session-Level Style Enforcement section added v0.110.0)
- Content-drift resync 2026-08-29 (v1.1.50): added a note on the v2.1.238 output-style mid-session drift fix — this repo's default `korean-engineer` output style is directly affected, since prior CC versions could silently drift the active style during a long session; also noted v2.1.237's new built-in Concise output style as a platform-native alternative preset.
- Content-drift resync 2026-09-18: reflected Claude Code v2.1.269 release notes (`/output-style [name]` mid-session list/switch, including Remote Control and cloud/headless sessions, as a direct remedy for the v2.1.238 drift issue).
- Content-drift resync 2026-09-18 (#1688): source's v2.1.238 note register was rewritten from 반말 to 합쇼체 (no new facts); this page's paraphrase was already English and required no wording change.
