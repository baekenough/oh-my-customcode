---
title: "R011: Memory Integration Rules"
type: rule
updated: 2026-09-24
sources:
  - .claude/rules/SHOULD-memory-integration.md
related:
  - [[r010]]
  - [[r013]]
  - [[r016]]
  - [[r020]]
  - [[r021]]
---

# R011: Memory Integration Rules

Defines when and how agents persist knowledge across sessions using native auto-memory.

> This project uses **native auto-memory ONLY** (MEMORY.md + agent frontmatter `memory:`). The claude-mem and agentmemory MCP servers are NOT used — they were permanently removed (#1253).

## Overview

Native auto-memory (agent frontmatter `memory` field) is the sole persistence mechanism — it loads the first 200 lines of MEMORY.md into the system prompt. Default paths are overridable via the `autoMemoryDirectory` setting (v2.1.74+). There are no external MCP memory backends. Memory failures must never block the main task.

## Priority

**SHOULD** — Strong recommendation. Missing memory integration reduces cross-session effectiveness but does not block operations.

## Key Requirements

**Architecture:** Native auto-memory is the single backend. No external MCP dependencies.

**Memory scope:**

| Scope | Location | Git Tracked |
|-------|----------|-------------|
| `user` | `~/.claude/agent-memory/<name>/` | No |
| `project` | `.claude/agent-memory/<name>/` | Yes |
| `local` | `.claude/agent-memory-local/<name>/` | No |

**Session-end auto-save responsibility:**

| Action | Owner |
|--------|-------|
| Session summary + native MEMORY.md update | `sys-memory-keeper` |

**Best practices:**
- Consult memory before starting work; update it after discovering new patterns
- Keep MEMORY.md under 200 lines
- Do not store secrets or duplicate CLAUDE.md content
- Memory write failures are non-blocking

## Subagent memory:project Source-Tree Pollution Guard

> Origin: #1335 ② — a `lang-kotlin-expert` with `memory: project`, working in a Kotlin source subdirectory, wrote memory to `mobile/.../com/baekenough/secondbrain/.claude/agent-memory/` — inside the source package. Caught and removed just before commit.

A subagent with `memory: project` that runs in a **subdirectory** can create `.claude/agent-memory/` relative to its cwd, polluting the source tree (e.g., inside a source package directory). Memory must resolve to the **project root** `.claude/`, never a nested working directory.

| Anti-pattern | Required |
|--------------|----------|
| Accept a subagent's `.claude/agent-memory/` written under a source package | Verify memory writes land at project-root `.claude/`; remove any nested `.claude/agent-memory/` before commit |

**Pre-commit check:** Before committing subagent work, scan for stray nested memory directories:

```bash
find . -path '*/src/*/.claude' -o -path '*/main/*/.claude'
```

## Attention-Weight Memory Tiering

> Origin: #1279 (Dual-Brain scout:internalize partial internalization)

Memory entries are managed on two orthogonal axes: confidence (verification level, see Confidence Lifecycle) and **attention weight** (access frequency/recency). Four tiers apply: **Hot** (2+ references this session — top of MEMORY.md), **Warm** (referenced within the last 3 sessions — below Hot), **Cold** (3+ sessions unreferenced but still valuable — moved to `sessions_archive_*.md` with an index line only), **Archived** (90+ days unreferenced per Temporal Decay — index-only). Access promotes a tier immediately (Cold → Warm → Hot); non-access demotes it at session end (Hot → Warm after 1 session, Warm → Cold after 3, Cold → Archived once Temporal Decay's 90-day threshold is also met). `[permanent]`-tagged entries are exempt from both decay and demotion. `sys-memory-keeper` re-scans at session start (archive-eligibility) and session end (demotion + archive move), keeping the 200-line budget resident to Hot/Warm content while Cold/Archived stay index-only.

**Standard delegation wording for sys-memory-keeper (#1660):** when delegating a mid-session memory update, the brief must state "no compaction/cleanup/tier re-evaluation needed — record only the listed items" (split cleanup into a separate delegation if it's actually needed). Without this line, the agent adds MEMORY.md compaction as a self-imposed goal and burns its turn budget doing it — the v1.1.63 iteration hit two 15-turn truncations this way (the memory-delegation angle of [[r020]]'s `maxTurns` truncation findings). The Hot/Warm/Cold promotion and archive-move work above is performed as its own single-goal delegation at session end, not folded into an in-session record-this-item request.

## Mid-Session Immediate Save

Save memory IMMEDIATELY upon surprising discovery — do not defer to session end.

| Trigger | Action | Rationale |
|---------|--------|-----------|
| Repeated pattern observed (2nd time) | Save `feedback_*.md` now | Pattern will recur within session |
| Unexpected tool behavior / workaround | Save `feedback_*.md` now | Session state defense |
| Subagent false-positive detected | Save `feedback_*.md` now | Prevent repeat in same session |
| User correction / feedback | Save `feedback_*.md` now | Honor correction immediately |
| Root-cause hypothesis (not yet verified against code) | Save `feedback_*.md` immediately with a `[hypothesis: <unverified-basis summary>]` first-line tag, never as confirmed fact, until the code path is read and its verdict logic reproduced 1:1 | Statistical correlation is not code causation ([[r020]] Diagnostic Hypothesis Verification) — premature "confirmed" framing risks the wrong premise persisting; #1652 #1 |

**`[hypothesis: …]` tag convention (#1652 #1):** the literal is unified to a single prefix form — `[hypothesis: <unverified-basis summary>]` as the first line of the memory body — not the bare `[hypothesis]` tag used in earlier drafts. A root-cause claim drawn from transcript/log statistics (correlation) rather than reading the actual code path and reproducing its verdict logic is saved under this tag, not as a confirmed fact. On verification (code path read + decision logic reproduced 1:1), the tag is removed or the file corrected; entries that still carry the tag are used only as a **verification trigger** in future sessions, never as a premise — the same principle as Safety-Related Feedback Memory Framing (below): conclusion-form, not verification-obligation form, is what's forbidden. Whether an entry is still tagged is checked deterministically with `grep -l '^\[hypothesis:' <memory dir>`, not by recall.

**Demonstrated (v1.1.59–60 sessions):** a root-cause claim for #1643 was saved as confirmed purely from transcript statistics (13/21 thinking-only first records) — "the advisor fails to merge thinking-only first records" — and was refuted by a 1:1 `jq` reproduction (the actual cause was label-wording ambiguity, not a merge failure). In the same session, a `gh pr merge --delete-branch` local side-effect claim was likewise saved as "confirmed" from 3 reflog observations, then failed to reproduce on a 4th merge and had to be corrected to "cause unconfirmed". **First applied target:** `feedback_gh_merge_delete_branch_local_side_effect.md` (the item corrected to "cause unconfirmed" in the v1.1.60 session) — sys-memory-keeper attaches the `[hypothesis: …]` tag to it at v1.1.61 session close.

**Why Immediate (not session-end):** Session-end saves lose context: by the time the session ends, multiple discoveries have compounded and nuance is lost. Immediate saves preserve the exact trigger context that makes the memory actionable.

**Anti-pattern**: "I'll batch all learnings at session end" — by then you'll have forgotten WHY each one mattered, and further violations may have occurred using the un-saved pattern.

## Procedure-Summary Scope Tagging (#1563)

When a procedure or ordering is compressed into memory, tag the **scope it applies to**. Compression discards contextual boundaries first, so an ordering that governs one sub-stage gets read as the ordering of the whole pipeline. A parenthetical is enough — and stating what the summary does *not* cover removes the ambiguity entirely.

| Anti-pattern | Required |
|--------------|----------|
| `release branch first → version bump → PR` (untagged → misread as whole-pipeline ordering) | `Ordering within the release stage: release branch first → version bump → PR (implementation commits go straight to develop)` |

Origin: #1563 찐빠 #5 — that exact summary described the ordering *inside* the release stage but was read as the ordering of the entire pipeline. Cross-ref: [[r013]] (Compact Output — be aware of what compression discards), Mid-Session Immediate Save above (preserving trigger context).

## Safety-Related Feedback Memory Framing

> Origin: #1307 — a delegation prompt framed a learning as "conclude it's a false positive and proceed," tripping the memory-poisoning safety classifier.

Safety-related feedback memories MUST use **verification-obligation form** ("this pattern triggers a duty to verify X; proceed only if verified, else STOP") — never **conclusion form** ("ignore the warning, it's a false positive"). Conclusion-form framing risks future sessions dismissing genuine threat warnings as memory-poisoning noise; writing triggers + STOP conditions instead preserves the warning's fail-safe intent.

## Session-End Retrospective Feedback (Model-Drafted)

Since `omcustom-feedback` is model-invocable (#1227), at session end the model MAY draft a retrospective feedback issue summarizing notable session friction or learnings — instead of only prompting the user to file it manually.

**Trigger conditions**: session-end detected AND notable friction/learning observed AND `omcustom-feedback` skill active.

**Key constraint**: The model NEVER auto-submits. Every draft passes through the skill's Phase 4A preview + confirmation gate before any GitHub issue is created. The user approves before publication.

| Path | Who Drafts | Who Approves |
|------|-----------|-------------|
| Manual (existing) | User | User (self-review) |
| Model-drafted (new) | Model | User via Phase 4A gate |

This complements — does not replace — the existing self-check item "prompt user to trigger omcustom-feedback". The Session-End Self-Check wording was updated to reflect that the model MAY draft instead of only prompting.

See [[omcustom-feedback]] for Phase 4A gate details.

## Platform Notes (v2.1.228/232+)

**Session cleanup was deleting project memory folder contents (fixed v2.1.228+).** On older CC, MEMORY.md and archive files could be lost during the session-cleanup step. Diagnostic consequence: a missing memory entry is not necessarily a *write* failure under the Enforcement section's failure policy below — the write may have succeeded and cleanup may have removed it. Per the Memory Scopes table above, the `project` scope (`.claude/agent-memory/`) is git-tracked and therefore recoverable; `user` and `local` scopes have no recovery path.

**Transcript relocation corruption fixed (v2.1.251+).** A directory-change could previously cause a session to re-layout onto an existing transcript with the same ID, corrupting or losing it — the same data-integrity class as the v2.1.228 session-cleanup deletion fix above. Sessions from before this fix may have counted violations (R020's transcript-parsing self-count) against an already-incomplete original; be especially wary of this when a session used `/cd` to change directories.

**Cowork sessions do not inline external @-imports in user-scope memory (v2.1.232+).** If a `~/.claude/agent-memory/` MEMORY.md pulls content in via `@`-import, that content may be absent from the context depending on session kind. Anything that must always be loaded belongs in the **body** of MEMORY.md rather than behind an import reference — consistent with the Attention-Weight Tiering rule of keeping Hot/Warm content inside the 200-line budget.

**MEMORY.md truncation-warning detail + `$`-sequence compaction corruption fix (v2.1.268+).** The MEMORY.md truncation warning now shows **how many lines were cut and where truncation started** — a deterministic trigger for the Attention-Weight Tiering rule above: when this warning fires, move Cold entries to archive. Separately, `/compact` and auto-compact summaries were corrupting text containing `$` sequences — on older CC, a compaction summary containing a shell snippet (`$?`, `${PIPESTATUS[0]}`, `$PPID`) may have been damaged, so recalling "what command actually ran" from a post-compact summary is not ground-truth (cross-ref [[r005]] zsh/`$?` note, [[r020]]). Also fixed: restored-file notes now load in the same order every time a `/compact`-ended conversation is resumed.

**Post-compact duplicate background processes, stale read-tracking, and synced-skill leftovers fixed (v2.1.271/273+).** (271) A background command still running after compaction (watch task, dev server) was spawning a **duplicate copy** on resume — on older CC, a post-compact duplicate process was a compaction artifact, not an instruction (cross-ref [[r010]] dev-server tmux hard block). (271) `/resume`/`/teleport` retained the previous conversation's file-read tracking, letting a resumed conversation edit a file it never actually read (cross-ref the [[r005]] v2.1.228 read-before-write note). (271) claude.ai-synced skills remaining on disk after logout is fixed — copies not refreshed within `cleanupPeriodDays` now move to a recoverable trash instead. (273) `permissions.blockReadsOutsideWorkingDirectories`: a repo settings-designated memory directory is no longer loaded into the prompt, recalled, indexed, or used for memory extraction — relevant to this rule's `autoMemoryDirectory` field: under this setting, a repo-designated memory directory is now excluded.

**Restored-memory age-note cache miss + malformed-transcript resume fixes (v2.1.275+, #1694).** A restored memory file's age note was changing between requests after a compaction or resume, causing prompt-cache misses — since this repo loads auto-memory MEMORY.md every session, some pre-275 post-compact/resume cache misses may have had this cause (treat as an [[r013]] `prompt_cache` cause candidate only, not confirmed). Separately, sessions failed to resume or start when their saved transcript contained a malformed message content block, and `--resume`, the resume-picker preview, resumed background agents, and the transcript view all failed on a session whose saved history contained a malformed task-reminder or @-file attachment entry — extends the v2.1.251 transcript-integrity note above; a pre-275 "resume failed" was not necessarily total session loss but could be a single malformed entry, so [[r020]] transcript-based counting on such a session is a lower bound, not a complete count.

## Enforcement

Prompt-based + Stop hook (soft block for session-end saves) per [[r021]]. Memory write failure remains non-blocking; **v2.1.210+** changed the failure mode — a write that pushes MEMORY.md past the read-limit budget now returns an explicit error instead of silently truncating, so recovery means reducing via Attention-Weight Tiering (archive Cold entries) before retry, not assuming truncation absorbed the overflow. (This version note was retired to an HTML comment in the source at v1.1.45 under the [[r016]] v2.1.212 retention floor — the oversize-write recovery behavior it describes is unchanged, only its auto-injection cost was removed.)

## Relationships

- **Enforced by**: `sys-memory-keeper` agent, Stop hook
- **Related rules**: [[r010]], [[r013]], [[r020]]
- **Affects**: All agents with `memory` frontmatter, session-end workflows

## Sources

- `.claude/rules/SHOULD-memory-integration.md` — rule definition (native auto-memory only, claude-mem/agentmemory removed #1253)
- Issue #869 — mid-session immediate save triggers (2026-04-15)
- Issue #1253 — claude-mem + agentmemory permanently removed (2026-05-29)
- Issue #1279 — Attention-Weight Memory Tiering (hot/warm/cold/archived)
- Issue #1307 — Safety-Related Feedback Memory Framing (verification-obligation form)
- Issue #1563 (v1.1.44, 2026-08-10) — Procedure-Summary Scope Tagging: compressed procedure summaries must carry their applicable scope, including what they do not cover
- Content-drift resync 2026-08-15 (v1.1.46) — added the Platform Notes section: v2.1.228 session-cleanup memory-folder deletion fix (missing memory ≠ write failure; `project` scope is git-recoverable, `user`/`local` are not) and v2.1.232 Cowork user-scope @-import non-inlining (keep always-needed content in the MEMORY.md body)
- Content-drift resync 2026-08-10 (v1.1.45) — the v2.1.210 oversize-memory-write note was HTML-comment retired in the source under the [[r016]] v2.1.212 version-note retention floor; content preserved verbatim inside the comment, no behavioral clause changed
- Content-drift resync 2026-08-29 (v1.1.50) — added the v2.1.251 transcript relocation/corruption fix note to Platform Notes: a directory change could re-layout a session onto an existing transcript ID, corrupting it — retrospective violation counts from before this fix may have parsed an incomplete original
- Content-drift resync 2026-09-03 (v1.1.60, #1652 #1) — added a "Root-cause hypothesis" row to Mid-Session Immediate Save and the `[hypothesis]` tag convention: a root-cause claim drawn from statistical correlation must be tagged `[hypothesis]` rather than saved as confirmed fact until the code path is read and its verdict logic reproduced 1:1 ([[r020]] Diagnostic Hypothesis Verification cross-ref).
- Content-drift resync 2026-09-03 (v1.1.61): unified the tag literal to the single prefixed form `[hypothesis: <unverified-basis summary>]` (was bare `[hypothesis]`), added the deterministic lookup expression `grep -l '^\[hypothesis:' <memory dir>`, and recorded the first applied target memory file `feedback_gh_merge_delete_branch_local_side_effect.md`.
- Content-drift resync 2026-09-18: reflected Claude Code v2.1.268~274 release notes (MEMORY.md truncation-warning line/location detail + `$`-sequence compaction-corruption fix, post-compact duplicate background-process fix, `/resume`/`/teleport` stale file-read-tracking fix, synced-skill leftover cleanup, and `blockReadsOutsideWorkingDirectories` excluding repo-designated memory directories from recall).
- Content-drift resync 2026-09-18 (#1694): reflected Claude Code v2.1.275 release notes — restored-memory-file age-note prompt-cache-miss fix (cause candidate only, cross-ref [[r013]]) and two malformed-transcript resume-failure fixes (extends the v2.1.251 transcript-integrity note; pre-275 resume failure on such a session is a lower bound for [[r020]] transcript counting, not total loss).
- Content-drift resync 2026-09-18 (v1.1.67, #1660): added the standard delegation-wording paragraph for sys-memory-keeper — mid-session memory-update briefs must say "no compaction/cleanup/tier re-evaluation needed", since omitting it let the agent add compaction as a self-imposed goal and hit two 15-turn `maxTurns` truncations in the v1.1.63 iteration.
- Content-drift resync 2026-09-24 (v1.1.77, #1717): rule body condensed under a new R016 instruction-budget policy — long CC version-note narratives and rationale wrapped in `<!-- DETAIL: ... -->` HTML comments (content preserved for Read, no longer auto-injected), each replaced with a short visible summary line; norms unchanged. New CC release knowledge now lands in `guides/claude-code/15-version-compatibility.md` per rule instead of accumulating inline (see [[r016]]).
