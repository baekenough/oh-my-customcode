---
title: "R023: Verification Ladder Rules"
type: rule
updated: 2026-09-19
sources:
  - .claude/rules/SHOULD-verification-ladder.md
related:
  - [[r021]]
  - [[r013]]
  - [[r009]]
  - [[r017]]
  - [[r020]]
  - [[deep-verify]]
  - [[adversarial-review]]
  - [[dev-review]]
  - [[evaluator-optimizer]]
  - [[worker-reviewer-pipeline]]
---

# R023: Verification Ladder Rules

검증을 비용/속도 사다리로 구성한다. 결정론적 검사가 먼저 통과해야 LLM 검토로 이어진다.

## Overview

가장 저렴한 tier가 잡을 수 있는 오류를 비싼 tier로 보내지 않는다는 shift-left 원칙. hooks와 linter가 잡을 수 있는 문제를 sonnet/opus에 보내는 것은 낭비다. [[r013]]의 출력 토큰 절약 원칙(Compact Output — 중간 단계·장황한 설명 생략)과 정합: ecomode는 출력 토큰을, R023은 검증 비용을 절약한다.

## Priority

**SHOULD** — advisory. 새 검증 도구 추가 시 tier 분류가 권장된다.

## Ladder Tiers

| Tier | 도구 | 비용 | 속도 | 적용 시점 |
|------|------|------|------|-----------|
| **1: Deterministic** | hooks, linters, type-check, JSON schema | $0 | <1s | Pre-write, write-time |
| **2: Cheap LLM** | haiku-based skills (`dev-review`, `action-validator`) | $ | <30s | Per-file review |
| **3: Expensive LLM** | sonnet/opus skills (`deep-verify`, `adversarial-review`, `multi-model-verification`, [[evaluator-optimizer]]) | $$$ | 1-5분 | Pre-commit, PR review |
| **4: Human** | maintainer review | time | hours-days | Final gate |

**`/code-review` runs as a background agent at every effort level (v2.1.232+).** Previously the high/xhigh/max levels ran in the foreground, so "the call returned" doubled as "the review finished". Now a Tier 3 verification call's return is a *start* signal, not a result — measure the background agent's actual completion and findings before advancing to the next tier or declaring a pass. Advancing the ladder on the old assumption grades an unfinished verification as passed ([[r020]] "attempt ≠ outcome"; cross-ref [[r010]] non-teammate background-by-default spawning).

## Asset Mapping

| Tier | 자산 |
|------|------|
| Tier 1 | `.claude/hooks/` PreToolUse hooks, `mgr-sauron` (R017 구조 검증), pre-commit linters |
| Tier 2 | [[dev-review]], [[action-validator]], `pre-generation-arch-check` |
| Tier 3 | [[deep-verify]], [[adversarial-review]], `multi-model-verification`, [[evaluator-optimizer]], [[worker-reviewer-pipeline]] |
| Tier 4 | maintainer PR approval |

## Relationship with R021

[[r021]]과 **직교**: R021은 위반 시 어떻게 멈출지(Hard Block / Advisory)를 정의하고, R023은 어떤 비용 순서로 검증할지를 정의한다. 같은 도구가 두 규칙에 동시에 속할 수 있다 — `mgr-sauron`은 R021 관점에서 Advisory, R023 관점에서 Tier 1이다.

## Integration

- **[[r009]]**: Tier 1-2 검사는 독립 파일에 대해 병렬 실행 가능
- **[[r013]]**: 컨텍스트 압박 시 Tier 3를 Tier 2로 다운그레이드 고려
- **[[r017]]**: Phase 1-3 검증 단계가 R023 Tier 1-3에 대응
- **[[r021]]**: 직교 — blocking 방식 vs 검증 비용 순서

## Safety-Signal Rule Authoring — Carve-Out Pre-Check (shift-left)

**트리거**: 런타임 안전-신호 동작을 정의하는 룰(인터럽트·취소·halt·중단 등)을 새로 작성하거나 수정할 때 적용한다. 이 checklist 자체 같은 메타-룰이나 구조/형식 변경에는 적용하지 않는다.

해당 룰 작성 시 Tier 3(적대적 검증)이 잡는 결함을 Tier 1(작성 시점)으로 시프트하기 위한 사전 점검. 불확실한 경우 이 Tier 1 pre-check를 먼저 통과한 뒤 Tier 3 검증으로 진행한다(ladder 순서 유지).

새 안전-신호 룰을 작성하거나 기존 룰을 수정하기 전, 아래 4개 질문을 통과해야 한다:

| 점검 항목 | 질문 |
|-----------|------|
| Fail-closed 필요 여부 | 이 신호가 오독되면 파괴적 작업이 계속 실행되는가? |
| "진행" 오독 여지 | 룰 문구에서 신호가 "계속" 허가로 읽힐 수 있는가? |
| Fail-safe 약화 여부 | 기존 fail-safe 조건(R001/R002 우선순위)을 약화하는가? |
| R001/R002 우선순위 명시 | R001 파괴적-작업 carve-out이 이 룰보다 우선함을 명시했는가? |

모든 항목 통과 전까지 룰을 병합하지 않는다. 이 pre-check가 없으면 Tier 3 적대적 검증이 뒤늦게 결함을 발견하게 된다.

**[[r016]]과 직교**: R016은 위반 *후* 룰을 업데이트하는 반응적 루프이고, 이 R023 carve-out pre-check는 룰을 *작성할 때* 수행하는 사전 점검이다. 두 규칙은 서로 다른 시점을 다루며 충돌하지 않는다.

> Origin: #1353 — #1341(FSD 인터럽트 오인 회고)의 후속 회고에서 발견된 R001 carve-out 누락. FSD 인터럽트 처리 룰 초안에서 "인터럽트 = 취소 오인"이 Tier 3 검증에서만 잡혔다. 작성 시점 Tier 1 사전 점검으로 시프트.

## Deprecated-Platform-Feature Staleness Check

staleness/audit 검증은 model ID·placeholder·TBD에만 한정하지 않고 **폐기된 플랫폼 기능·설정·절차 참조**까지 grep 스캔해야 한다. 실례로 CC v2.1.121에서 폐기된 `/tmp/*.sh` script bypass 절차가 9개 에이전트 본문에 잔존했으나(v1.1.0에서 제거), 감사 staleness dimension이 model-ID/placeholder에만 국한되어 이를 놓쳤다. 감사·staleness 체크리스트에 "CC 특정 버전에서 폐기된 기능/설정/절차를 현행처럼 참조하는가"를 항목으로 포함한다. 저렴한 결정론적 grep으로 폐기 참조를 조기 탐지하는 것은 R023 shift-left 원칙과 정합한다.

| Anti-pattern | Required |
|--------------|----------|
| staleness 스캔을 model ID/placeholder/TBD로만 한정 | 폐기된 플랫폼 기능/절차 참조(deprecated CC feature/procedure)도 grep 스캔 |

> Origin: #1433 #3 (Low) — CC v2.1.121에서 폐기된 `/tmp/*.sh` bypass 절차가 9개 에이전트 본문에 잔존했으나 감사 staleness dimension이 model-ID/placeholder에만 한정되어 놓친 사례.

## Fable 5 Over-Prescription Advisory

Fable 5 실행 에이전트/스킬은 지시(instruction) 장문화가 오히려 품질을 저하시킬 수 있다(too prescriptive). 신규 규칙/스킬 추가 시 간결성을 우선하고, Fable 5 실행 대상 문서의 과잉처방을 경계한다. 상세는 `guides/claude-code/16-fable5-prompting.md` 참조.

> Origin: #1435 (scout INTERNALIZE — Fable 5 공식 프롬프팅 가이드).

## Sample-Value Assembly Local Verification

워크플로우/스크립트 내 문자열 조립·템플릿 로직(heredoc, f-string, 다중라인 변수 삽입, 이슈 본문 조립 등)의 수정을 위임할 때, 위임 프롬프트는 **API/네트워크가 불필요한 순수 조립부를 Tier-1 로컬 결정론 검증으로 분리**하도록 명시해야 한다. 문법 검증(py_compile/YAML lint/`node --check`)은 통과해도 조립된 출력의 선행 들여쓰기·형식 결함은 드러나지 않는다 — 샘플 값(멀티라인 변수 포함)으로 출력을 실제 조립한 뒤 선행 들여쓰기·형식을 grep으로 검증해야 잡힌다.

| Anti-pattern | Required |
|--------------|----------|
| "스모크 테스트 = API 호출"로 협소 인식 → 키 불필요한 순수 문자열 조립 검증을 실서버 dispatch Tier로 미룸 | 위임 첫 프롬프트부터 "샘플 멀티라인 값으로 출력 실제 조립 후 선행 들여쓰기·형식 grep 검증"을 필수 항목으로 명시 |
| `textwrap.dedent(f"""...{multiline_var}...""")` 를 신뢰 | 삽입 변수가 0-indent 멀티라인이면 dedent 공통 최소 들여쓰기가 0으로 계산되어 무력화 → 리터럴 들여쓰기 잔존. dedent 제거 후 명시적 문자열 concatenation 사용 |

### 키·해시 설계 위임서 — 저장소 대표 편집 픽스처 선확인 (Origin: #1691 #1, v1.1.71)

편집·상태를 식별하는 키나 해시의 설계를 위임할 때, 완료 조건에 **이 저장소의 대표 편집 3종**(룰 노트 삽입, 카운트 범프 `115→116`, 버전 범프 `1.1.68→1.1.69`)을 픽스처로 넣어 충돌 여부를 먼저 확인하도록 명시한다. v1.1.68에서 `<len>#<head80>#<tail40>` 키를 확정형으로 지시했다가, 120자 초과 Write에서 중간 구간만 바뀌는 동일 길이 편집(바로 그 카운트·버전 범프)이 충돌한다는 사실을 적대적 리뷰가 실증해 코드포인트 합을 추가했다 — 작성 단계(Tier 1)에서 대표 입력으로 검증하면 Tier 3 비용이 들지 않는다.

| Anti-pattern | Required |
|--------------|----------|
| 키·해시 형식을 확정형으로 지시하고 충돌 사례를 열거하지 않음 | 저장소 대표 편집 3종 픽스처로 충돌 선확인을 완료 조건에 포함 |

> Origin: #1691 #1 (v1.1.71).

**Common Violation (#1455):** cc-release-monitor PR #1449 머지 후 workflow_dispatch 실검증에서 issue_body의 `<details>`·릴리즈 요약에 12칸 리터럴 들여쓰기 발견 → PR #1451 재작업. 첫 위임이 문법 검증만 지시하고 샘플 값 출력 조립 검증을 누락. `textwrap.dedent` + 멀티라인 변수 함정이 문법 검증만으로는 미노출. [[r020]](문법 통과 ≠ 출력 정상)과 정합.

> Origin: #1455 #1 (Session 127 회고 찐빠 #1).

## Delegated Verification Floor — CI 잡 목록에서 도출 (#1574)

위임 프롬프트의 검증 항목을 "변경 파일의 영향 범위"만으로 정하면 부족하다. **하한선은 CI가 실제로 돌리는 잡 전체**다 — 워크플로 YAML의 잡 목록을 읽어 대응하는 로컬 명령(`lint` / `test` / `validate-docs` / sync 검사)을 열거하고, 그중 로컬 실행 가능한 것을 위임 완료 조건에 포함한다. 로컬에서 통과시키지 않은 CI 잡은 병합 시점에 halt로 돌아와 수정 에이전트 추가 발주를 강제한다 — 즉 누락 비용은 "나중에 알게 됨"이 아니라 "라운드트립 1회"다.

| Anti-pattern | Required |
|--------------|----------|
| "변경분 영향 범위"만 보고 검증 항목을 정해 위임 → CI 전용 잡(lint 등) 누락 | 워크플로 잡 목록을 하한선으로 삼아 로컬 대응 명령을 완료 조건에 열거 |

**Common Violation (#1574):** v1.1.44 세션에서 병렬 위임 3건이 모두 `bun run lint`를 누락해 verify-build halt → 수정 에이전트 1회 추가 발주. 기존 `feedback_delegation_verify_scope_by_impact`("영향 범위 기준")의 하한선을 명문화한 조항이다. Cross-ref: [[r020]] (완료 검증 — 선언 전 실제 게이트 통과 확인), [[r017]] (커밋 전 검증 게이트).

### 상한선 — 오케스트레이터 사전 실측 항목은 재실행 금지 (#1655)

위 Delegated Verification Floor는 **하한선**(CI 잡 목록에서 도출)을 규정한다. 대칭으로 **상한선**도 존재한다 — 오케스트레이터가 위임 전에 이미 실측을 완료한 항목을, 위임서에 "재검증하라"로 다시 포함시키지 않는다. 재실행은 절대 무해하지 않다 — 다중 Phase 위임의 턴 예산을 불필요하게 소모해 [[r020]] maxTurns 절단 위험을 키운다.

실증: mgr-sauron 위임 2건을 대조했다 — 오케스트레이터가 이미 실측한 항목(파일 존재·카운트 등)을 위임서에 다시 "확인하라"로 포함시킨 위임은 **25턴에서 절단**됐고, 동일 검증 대상에서 그 항목을 제외하고 미실측 항목만 위임한 쪽은 **16턴에 완주**했다. 재검증 지시가 곧 턴 예산 낭비였다.

| Anti-pattern | Required |
|--------------|----------|
| 오케스트레이터가 이미 실측 완료한 항목을 위임서 완료 조건에 "재확인" 형태로 포함 | 사전 실측 항목은 위임서에서 제외하거나 "이미 확인됨 — 재실행 불필요"로 명시; 위임은 미실측 항목에만 집중 |

Origin: #1655 (mgr-sauron 위임 대조 — 재검증 포함 25턴 절단 vs 제외 16턴 완주). Cross-ref: [[r020]] (maxTurns Truncation — 턴 예산은 편집 항목 수로 산정), 위 Delegated Verification Floor(하한선의 대칭).

## Conditional-Output Verification — Positive/Negative Pair Mandate

조건부로만 출력하는 대상(advisory 훅, 가드, 경고 emitter)의 동작 검증을 위임할 때, 완료 기준은 **"출력이 나와야 하는 입력"과 "나오면 안 되는 입력"을 짝으로** 지정해야 한다. "stdout ≠ 0바이트" 같은 단일 프록시는 검증이 아니다 — 침묵이 정답인 입력에서도 통과를 요구하게 되어 기준 자체가 성립하지 않고, 반대로 준수 턴에서 발화하는 오탐을 통과시킨다.

| Anti-pattern | Required |
|--------------|----------|
| "지정 입력에서 stdout ≠ 0바이트"를 단일 완료 기준으로 위임 | 양성 케이스(발화해야 함)와 음성 케이스(침묵해야 함)를 짝으로 명시 |

**Common Violation (#1563):** R007/R008 advisor 발화 검증에 단일 "0바이트 아님" 프록시를 완료 기준으로 제시했으나, advisor는 준수 턴에서 침묵하는 것이 정상 동작이므로 기준 자체가 성립하지 않았다. 이 항목이 Tier-1(결정론적) 검증의 **기준 설계** 각도이며, 아래 Detection Guard Delegation Standard는 같은 positive/negative 구분을 **가드 패턴 설계** 각도에서 다룬다 — 짝을 이루는 조항이다.

> Origin: #1563 찐빠 #2. Cross-reference: [[r020]] (Proxy Signal vs Canonical Ground-Truth — 프록시로 상태를 특성화하지 말 것).

### 래퍼·재사용 스크립트 위임의 음성 픽스처 — 원본 전처리 필터 계열별 (#1688 — Iteration 4 #3)

기존 스크립트(훅·판정기)를 래핑하거나 그 판정을 재사용하는 스크립트를 위임할 때, 위임서는 **원본이 입력에서 제외하는 계열**(예: advisor의 `isSidechain` 레코드, `thinking` 블록, `agent_id` 세션 게이트)을 전부 열거하고 **계열별로 적용 또는 의도적 미적용을 판정해 사유를 기재**하도록 요구하며, **제외 계열마다 음성 픽스처**를 요구한다. 양성/음성 짝이 있어도 제외 계열이 픽스처에 없으면 "1:1 재현" 주장은 검증되지 않은 것이다 — v1.1.67에서 `scripts/count-r007-r008.sh`가 advisor의 사이드체인 필터를 누락해 사이드체인이 섞인 트랜스크립트에서 같은 턴을 이중 계상할 수 있었고, 픽스처가 전부 `isSidechain: false`라 적대적 리뷰 전까지 미탐지였다. 위 Conditional-Output Verification과 같은 계열이다.

| Anti-pattern | Required |
|--------------|----------|
| 래퍼 스크립트 위임서에 경계 규칙만 지정하고 원본의 전처리 필터 미열거 | 원본 제외 계열 전수 열거 + 계열별 적용/미적용 판정과 사유 + 계열별 음성 픽스처 요구 |

> Origin: #1688 — Iteration 4 #3. Cross-reference: 위 Conditional-Output Verification, 아래 Detection Guard Delegation Standard(positive/negative 문맥 구분).

## Detection Guard Delegation Standard

Tier-1 shift-left 검출 가드(예: deprecated-pattern grep 가드)의 설계·수정을 서브에이전트에 위임할 때, 위임 프롬프트는 **positive-match**(genuine defect mandate — `MUST`/`MANDATORY` 인접 문맥)와 **negative-context**(deprecation note — "no longer"/"deprecated"/"불필요"/"폐기됨" 설명 문구)를 구분하도록 명시해야 한다. 이를 누락하면 올바르게 수정된 파일의 폐기-설명 문구까지 과잉매칭하여 자기모순 BLOCK을 유발한다.

| Anti-pattern | Required |
|--------------|----------|
| bare 패턴(`/tmp/*.sh` 등)만 grep하도록 위임 → 올바르게 수정된 파일의 "폐기됨/불필요" 설명 문구까지 오탐 | 위임 프롬프트에 positive-match(MUST/MANDATORY 인접) vs negative-context(deprecated/no longer/불필요 설명) 구분 기준을 명시 |

**Common Violation (#1438):** deep-verify 가드 반전 위임 시 bare `/tmp/*.sh` 패턴이 올바르게 수정된 9개 파일의 "폐기됨/불필요" 설명 문구까지 오탐; `mgr-sauron`이 sha256 재계산으로 적발, 3-패턴(mandate/false-claim) 협소화로 정정.

> Origin: #1438 (Session 125 회고 찐빠 #3).

## Workflow Prompt & Verifier Ground-Truth

> Origin: #1266 ③ (High) — a Workflow built the agent prompt as `await agent(prompt) + FACTS`, concatenating the guardrail fact-sheet onto the RETURN VALUE instead of the prompt. The writer never received the facts, hallucinated an in-cluster hostname, and the adversarial verifier couldn't catch it (the fact was in no source it had).

**Prompt Completion Before Call:** Workflow/agent prompts MUST be fully assembled into the prompt string **before** the `agent()` / Agent tool call. Post-call concatenation onto the return value is a footgun — the agent never sees the appended content (`agent(prompt) + FACTS` is wrong; `agent(prompt + FACTS)` is required).

**Workflow Script Sanity Check** — before invoking a Workflow script, deterministically verify:

| Check | Why |
|-------|-----|
| No unresolved placeholders (`{phase1_summary}`, `TODO`, `<...>`, `{{ }}`) remain in any agent prompt string | An unfilled placeholder reaches the agent verbatim → garbled task |
| Assemble-before-call (see above) | Post-call concatenation silently drops content |
| Script parses — balanced braces/quotes, valid JS | A syntax error aborts the entire run after partial work |
| 프롬프트 문자열 내 셸 변수 `${...}`(`$?`, `${PIPESTATUS[0]}`, `$(...)` 등)가 `\${...}`로 이스케이프되어 있는지 사전 grep 확인 | JS 템플릿 리터럴 안의 이스케이프 안 된 셸 `${...}`를 JS가 JS 표현식으로 평가 → 런타임 `ReferenceError`. `node --check`는 문법만 검사하여 이 런타임 오류를 못 잡으므로 별도 결정론 grep 검사가 필요함 |
| Workflow `args`를 사용하는 스크립트가 `typeof args === 'string' ? JSON.parse(args) : args` 방어를 거친 뒤 필드에 접근하는지 확인 | 하니스가 객체 args를 문자열로 인코딩해 전달하면 `args.<field>`가 undefined가 되어 스크립트가 즉시 런타임 실패(0 agents 실행). `node --check`는 문법만 검사하므로 위 셸 `${...}` 이스케이프 항목과 동일한 런타임 계열을 잡지 못함 |

**Common Violation (#1271):** Session 106 follow-up to #1266 ③ — the guardrail fact-sheet was concatenated onto the agent's RETURN VALUE instead of the prompt string, uncaught because no pre-run sanity check existed. This check is the deterministic Tier-1 guard that catches such slips before the expensive run.

**Common Violation (#1438):** fix Workflow의 verify 프롬프트에 `${PIPESTATUS[0]}`를 이스케이프 없이 사용 → `PIPESTATUS is not defined` ReferenceError로 verify 단계 실패. `node --check`는 통과했으나 런타임에서 실패 — 셸 변수 이스케이프 grep 항목이 이 사례로 추가되었다.

**Common Violation (#1512):** v1.1.27 세션 회고 찐빠 #2 — Workflow `args`를 JSON 객체로 전달했으나 하니스가 문자열로 인코딩해 `args.paths`가 undefined → 0 agents 실행으로 즉시 런타임 실패. `typeof args === 'string' ? JSON.parse(args) : args` 방어 추가 후 재실행 성공.

**MCP empty-schema args-as-string bug (v2.1.246+):** when an MCP tool's parameter schema is an empty object (`{}`), arguments were previously delivered as a **JSON string** instead of their actual type — this is fixed. This confirms the **same bug class** as the `args` JSON.parse defense row above (#1512) also existed on the MCP tool-call path: calling an MCP tool with a `{}` schema on older CC could deliver its arguments as a string, immediately failing with `args.<field>` undefined. The `typeof args === 'string' ? JSON.parse(args) : args` defense was needed by the same logic for code wrapping empty-schema MCP tools, not just Workflow scripts.

**Dynamic `import()` sandbox escape (v2.1.223+):** a workflow script could use dynamic `import()` to execute code **outside the workflow sandbox**; this is fixed. The sanity-check table above covers prompt assembly, syntax, and runtime classes — but never covered **sandbox escape**, and on older CC a script could pull in out-of-boundary code while passing `node --check` and assembling its prompts correctly. Before running an externally sourced workflow script, grep it for dynamic `import()` usage (a Tier-1 deterministic check).

**Verifier Ground-Truth for Cross-Cutting Facts:** Cross-cutting facts not verifiable from the primary source (external URLs, in-cluster DNS/hostnames, infra topology) MUST be supplied to the verifier as explicit ground-truth. Otherwise an adversarial verifier cannot distinguish a hallucinated value from a correct one — a verification blind spot. Cross-reference: [[r009]] (giant-prompt decomposition), [[worker-reviewer-pipeline]] skill.

**Workflow tool robustness (v2.1.259/260+):** three fixes. (259) resuming a workflow run whose previously-stopped execution was still exiting could **duplicate-run** its agents — so `resumeFromRunId` right after a stop was not safe; the correct order is stop → confirm the process actually exited → resume. (260) Workflow `agent({schema})` now **pre-rejects unsatisfiable JSON Schemas** and includes the last validation failure in the retry-limit-exceeded error — a platform-performed Tier-1 deterministic pre-check, extending the Workflow Script Sanity Check table above with "an unsatisfiable schema is now rejected before any agent runs." (260) a Workflow subagent stuck in a long context-compaction was no longer restarted as "stalled" — cross-ref [[r009]] Adaptive Parallel Splitting: waiting on compaction is not a stall.

**CC v2.1.267+:** in auto mode, a Workflow `agent()` call with a **large output schema** was previously **rejected** outright instead of being routed through the safety classifier's review — on older CC that rejection was a platform size constraint, not a classifier verdict, so it should NOT be counted toward [[r010]]'s Subagent Scope-Creep STOP Protocol trip count. Separately, v2.1.261 added `bashOutputMaxChars`/`taskOutputMaxChars` settings (up to 128K chars) controlling how much command/background-task output reaches the model inline before being written to a file — relevant when a Tier-1 verification script's output gets truncated to a file and its pass/fail line goes missing; prefer reading the exit code alone (R005 #1492) over raising the limit.

**`claude plugin eval` + Workflow concurrency setting + delegated-verification observability (v2.1.269+):** `claude plugin eval` runs a plugin's eval suite through Claude Code itself, producing scored, reproducible results (JSON + HTML report) — a new Tier-1/2 tool for skill-quality verification in this ladder (cross-ref `skill-creator`, [[r006]]). `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS` (1-256) is a new setting for the Workflow tool's per-run agent concurrency cap — the platform ceiling is now configurable, but this repo's [[r009]] policy ceiling (5 concurrent, soft 4) still applies. `--output-format stream-json`'s `permission_denials` now also includes Read/Edit/Write calls blocked by path-scoped deny rules — on older CC these denials may have been missing from `-p` verification-run denial telemetry. `bashEditDiffEnabled` attaches a diff of files a Bash command changed to the Bash tool result, enabling deterministic Tier-1 post-hoc checking of shell-mediated edits.

**Content-addressed plugin approval + workflow pause-on-limit + notification/MCP-startup improvements (v2.1.271/274+):** (271) `claude plugin install`/`update` gained `--accept-command <sha256>` — approving exactly the sha256 a prior `--json` run displayed, instead of a blanket `-y` — content-addressed deterministic (Tier 1) approval now outranks blanket yes. A dynamic workflow hitting a usage limit now **pauses and resumes when the limit resets** instead of dropping the agent (cross-ref the resume-related item in the Workflow Script Sanity Check note above). (274) Monitor tool notifications now bundle a script's final output and exit into a **single** notification (saves a model turn). `CLAUDE_CODE_MCP_STARTUP_WAIT_MS` caps how long the first non-interactive turn waits on a connecting MCP server — relevant when a Tier-1 `-p` verification run should not be delayed by MCP startup.

## Sources

- `.claude/rules/SHOULD-verification-ladder.md` — rule definition (2026-07-19 resync #1483: Overview R013 관계 문구 정합 — "저렴한 검증 우선" 일반 원칙 인용에서 "Compact Output — 중간 단계·장황한 설명 생략" 구체 인용으로 정정 [v1.1.11 하네스 감사 후속 정리 커밋 a56c9d5a에서 소스만 갱신되고 wiki는 미반영된 drift]; Ladder Tiers Tier-3 표에 `evaluator-optimizer` 누락 보정 [pre-existing drift, Asset Mapping에는 이미 존재]. Sample-Value Assembly Local Verification #1455 added 2026-07-06 + Fable 5 Over-Prescription Advisory #1435 + Detection Guard Delegation Standard #1438 + Workflow Prompt & Verifier Ground-Truth section including #1266③/#1271/#1438 shell-escape common violation added 2026-07-04; Workflow Script Sanity Check table gained an `args` JSON.parse defense row + Common Violation #1512 added 2026-07-20)
- Content-drift resync 2026-08-10 (v1.1.45, #1574): added Delegated Verification Floor — CI 잡 목록에서 도출 (the delegation verification floor is the CI job list, not the changed-file blast radius). Also a directional-reference fix in the source ("위" → "아래" for the Detection Guard Delegation Standard cross-ref), no wiki-visible change.
- Content-drift resync 2026-08-10 (v1.1.44, #1563): added Conditional-Output Verification — Positive/Negative Pair Mandate (paired positive/negative cases as the completion criterion for conditional emitters), and the v2.1.223 dynamic-`import()` workflow-sandbox-escape note to Workflow Prompt & Verifier Ground-Truth.
- Content-drift resync 2026-08-15 (v1.1.46): added the v2.1.232 note that `/code-review` runs as a background agent at high/xhigh/max effort too — a Tier 3 call's return is a start signal, so ladder advancement requires measuring the background agent's actual completion.
- Content-drift resync 2026-08-29 (v1.1.50): added the v2.1.246 MCP empty-schema args-as-string fix to Workflow Prompt & Verifier Ground-Truth — the same bug class as the #1512 Workflow-`args` JSON.parse defense, now confirmed to also affect the MCP tool-call path
- Content-drift resync 2026-09-03 (#1655): added "상한선 — 오케스트레이터 사전 실측 항목은 재실행 금지" under Delegated Verification Floor — a delegation prompt re-including an already-measured item cost a full mgr-sauron turn budget (25-turn truncation vs 16-turn completion with the item excluded), the symmetric ceiling to the floor's CI-job-derived lower bound
- Content-drift resync 2026-09-17: reflected Claude Code v2.1.259~267 release notes (Workflow tool robustness — resume duplicate-run guard, unsatisfiable-schema pre-rejection, compaction-is-not-a-stall fix (v2.1.259/260+); large-output-schema `agent()` rejection excluded from R010 trip count + `bashOutputMaxChars`/`taskOutputMaxChars` settings (v2.1.267+)).
- Content-drift resync 2026-09-18: reflected Claude Code v2.1.268~274 release notes (`claude plugin eval` Tier-1/2 skill-quality tool, `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS` setting, `stream-json` path-deny denial-telemetry fix, `bashEditDiffEnabled` (v2.1.269+); content-addressed plugin-install approval, workflow pause-on-usage-limit, Monitor notification merging, `CLAUDE_CODE_MCP_STARTUP_WAIT_MS` (v2.1.271/274+)).
- Content-drift resync 2026-09-19 (#1691): added "키·해시 설계 위임서 — 저장소 대표 편집 픽스처 선확인" under Sample-Value Assembly Local Verification — a delegation designing a key/hash for identifying edits or state should require checking collisions against this repo's 3 representative edit types (rule-note insertion, count bump, version bump) as fixtures before finalizing the format (v1.1.68 stuck-detector `edit_hash` `<len>#<head80>#<tail40>` collision on same-length mid-section edits, caught by adversarial review, fixed by adding a codepoint-sum term).
- `guides/compound-ai-workflow/README.md` — 원칙 3 (Verification Ladders) 컨텍스트
